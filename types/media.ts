export interface Movie {
      id: number;
      title: string;
      genre: string;
      releaseDate: string;
      // ... other movie properties
    }

    export interface TVSeries {
      id: number;
      title: string;
      genre: string;
      seasons: number;
      // ... other TV series properties
    }

    export interface Book {
      id: number;
      title: string;
      genre: string;
      author: string;
      // ... other book properties
    }
